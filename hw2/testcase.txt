# some test cases for hw2
# need to create pipes

ls

ls -l | grep .c

ls -l | wc -l

ps | grep  

cat <filename>

--

passed:

exit

cd <dirname>

ls

cat <filename>

debugging:

ps -ef | grep root (segmentation fault)







