export MYPATH=/usr/local/bin#/usr/bin#/bin#.

# some test cases for hw2
# need to create pipes

ls

ls -l | grep .c

ls -l | wc -l

ps | grep  

cat <filename>

exit

cd <dirname>

ls

cat <filename>

ps | less

cat | more


--

passed:

exit

ps -ef

cd








cat some_file | wc -l (in test.c) // need to have close(p[0]), close(p[1]) in parent as well
ps -ef | grep root (in test.c)  

debugging:




----

current issues:

1. pipe second cmd won't exit (check waitpid) ----> put close() in parent process
2. exit sometimes will execute twice to exit (check waitpid)










